version: '3'
services:
  app:
    build: .
    ports: ["8080:8080"]
    depends_on: ["db", "redis"]
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
  redis:
    image: redis:alpine